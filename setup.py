from setuptools import find_packages, setup

setup(
    name="spg-experiments",
    version="0.0.1",
    description="Description of my package",
    packages=find_packages(),
    install_requires=[],
)
